<%= form_for(player_award) do |f| %>
  <% if player_award.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(player_award.errors.count, "error") %> prohibited this player_award from being saved:</h2>

      <ul>
      <% player_award.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :player_id %>
    <%= f.number_field :player_id %>
  </div>

  <div class="field">
    <%= f.label :award_id %>
    <%= f.number_field :award_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
